---

netbox_version: 2.5.13

netbox_url:

netbox_database:
  hostname:
  username:
  password:
  db:

netbox_ldap_config:
  enabled: false

netbox_user: netbox

netbox_centos_packages:
  - gcc
  - graphviz
  - libxml2-devel
  - libxslt-devel
  - libffi-devel
  - nginx
  - openldap-devel
  - openssl-devel
  - python36
  - python36-devel
  - python36-pip
  - python36-virtualenv

netbox_ubuntu_packages:
  - python3-graphviz
  - openssl1.0
  - python3-dev
  - python3-pip
  - python3-virtualenv
  - nginx